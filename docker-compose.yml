version: '3.8'

services:
  web:
    build: .
    ports: 
      - "8080:5000"
    volumes:
      - ./instance:/app/instance
    environment:
      - FLASK_ENV=development
      - SECRET_KEY=watched-secret-key
